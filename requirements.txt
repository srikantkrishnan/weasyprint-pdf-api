fastapi==0.111.0
uvicorn==0.30.1
weasyprint==66.0
pyhanko[pyca]==0.29.1  # CORRECTED: Use the [pyca] extra to enable cryptography support
pyhanko-certvalidator==0.27.0
cryptography==45.0.5
lxml==6.0.0
Pillow==11.3.0
fonttools==4.59.0
python-multipart==0.0.20
pytz==2024.1
# You may also need to explicitly install pyhanko's pyca dependency if not done by default
# You should be able to do this with pip install 'pyhanko[pyca]'

# fastapi==0.111.0
# jinja2==3.1.6
# pyyaml==6.0.2
# typing-extensions==4.14.1

# Explicit optional deps for pyHanko + WeasyPrint stability
# oscrypto==1.3.0
# tzlocal==5.3.1
# orjson==3.11.1
# uvloop==0.21.0
# watchfiles==1.1.0
# pikepdf==8.12.0

# New deps for OpenSSL-based PDF signing overlay
# reportlab==4.0.9
# PyPDF2==3.0.1
# pymupdf==1.24.2   # PyMuPDF

# Timezone handling
# pytz==2024.1
