# Core FastAPI + ASGI server
fastapi==0.111.0
uvicorn==0.30.1

# PDF generation with WeasyPrint
weasyprint==66.0
lxml==6.0.0
Pillow==11.3.0
fonttools==4.59.0
jinja2==3.1.6
pyyaml==6.0.2

# Digital signatures with PyHanko
pyHanko==0.31.0
pyhanko-certvalidator==0.27.0
cryptography==45.0.5
oscrypto==1.3.0
asn1crypto==1.5.1   # required for PKCS#12 (PFX) parsing
tzlocal==5.3.1

# Incremental PDF writer (needed for PyHanko)
pikepdf==8.12.0

# Upload handling
python-multipart==0.0.20

# JSON performance + typing
orjson==3.11.1
typing-extensions==4.14.1

# Deployment stability (Render friendly)
uvloop==0.21.0
watchfiles==1.1.0
